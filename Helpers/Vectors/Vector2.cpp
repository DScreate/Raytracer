//
// Created by tessa on 1/23/20.
//

#include "Vector2.h"

Vector2 &Vector2::operator+(const Vector2 &that) {
    return <#initializer#>;
}

Vector2 &Vector2::operator-(const Vector2 &that) {
    return <#initializer#>;
}

float Vector2::dot(const Vector2 &that) {
    return 0;
}

Vector2 &Vector2::cross(const Vector2 &that) {
    return <#initializer#>;
}

Vector2 &Vector2::operator*=(float factor) {
    return <#initializer#>;
}

Vector2 &Vector2::operator/=(float factor) {
    return <#initializer#>;
}

Vector2::Vector2(const float x, const float y) {
    this->x = x;
    this->y = y;
}
